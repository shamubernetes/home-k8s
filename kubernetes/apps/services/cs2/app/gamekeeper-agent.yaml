# yaml-language-server: $schema=https://k8s-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cs2-gamekeeper
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: op-secret-store
  target:
    name: cs2-gamekeeper
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        token: "{{ .TOKEN }}"
  data:
    - secretKey: TOKEN
      remoteRef:
        key: The Zoo - Gamekeeper
        property: CS2_AGENT_TOKEN
---
apiVersion: v1
kind: Service
metadata:
  name: cs2-gamekeeper
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/controller: cs2
    app.kubernetes.io/instance: cs2
    app.kubernetes.io/name: cs2
  ports:
    - name: agent
      port: 3001
      protocol: TCP
      targetPort: agent
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cs2-gamekeeper
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/controller: cs2
      app.kubernetes.io/instance: cs2
      app.kubernetes.io/name: cs2
  policyTypes: [Ingress]
  ingress:
    - from:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - port: 27015
          protocol: TCP
        - port: 27015
          protocol: UDP
        - port: 27020
          protocol: UDP
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/instance: zoo
              app.kubernetes.io/name: gamekeeper
      ports:
        - port: 3001
          protocol: TCP
