# yaml-language-server: $schema=https://k8s-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: victoria-traces
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: victoria-traces
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  dependsOn:
  - name: rook-ceph-cluster
    namespace: rook-ceph
  - name: kube-prometheus-stack
    namespace: observability
  values:
    rbac:
      create: false
    serviceAccount:
      create: true
      automountToken: false
    server:
      fullnameOverride: victoria-traces
      image:
        registry: docker.io
        repository: victoriametrics/victoria-traces
        tag: v0.11.0@sha256:c36484821a368a87906c8b2fa34ef3993a8dad746e2b9b32aabb6432ff7c4ced
      retentionPeriod: 14d
      retentionDiskSpaceUsage: 25GiB
      extraArgs:
        envflag.enable: true
        envflag.prefix: VT_
        loggerFormat: json
        storage.minFreeDiskSpaceBytes: 2GB
      persistentVolume:
        enabled: true
        accessModes: ["ReadWriteOnce"]
        storageClassName: ceph-block
        size: 30Gi
      podSecurityContext:
        enabled: true
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
      securityContext:
        enabled: true
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        capabilities:
          drop: ["ALL"]
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          cpu: "2"
          memory: 2Gi
      service:
        clusterIP: ""
      serviceMonitor:
        enabled: true
        interval: 30s
      terminationGracePeriodSeconds: 120
