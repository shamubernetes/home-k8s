# yaml-language-server: $schema=https://json.schemastore.org/kustomization
# Verification only: automatic image queue A.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ./agent
- ./aggregator
