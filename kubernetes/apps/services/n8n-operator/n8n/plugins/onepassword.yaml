---
apiVersion: n8n.n8n.io/v1alpha1
kind: N8nPlugin
metadata:
  name: onepassword
  namespace: services
spec:
  instanceRef:
    name: n8n
  packageName: "@n8layer/n8n-nodes-1password"
  version: latest
  # Disabled: isolated-vm 6.1.0 has no Node 24 prebuilds, fails to compile in n8n image (no Python)
  enabled: false
