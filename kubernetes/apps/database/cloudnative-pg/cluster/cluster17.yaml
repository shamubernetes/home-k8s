# yaml-language-server: $schema=https://k8s-schemas.pages.dev/postgresql.cnpg.io/cluster_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres17
spec:
  instances: 3
  imageName: ghcr.io/cloudnative-pg/postgresql:17.11@sha256:26ca47f8c5ebe5dad7d7818474681f8c5fc8d1fcaa136c8a8d8f716904d683a1
  primaryUpdateStrategy: unsupervised
  # Use switchover (not restart) for faster, cleaner failovers
  primaryUpdateMethod: switchover
  # Shorter delays for faster failover
  switchoverDelay: 60
  failoverDelay: 0
  storage:
    size: 20Gi
    storageClass: ceph-block
  walStorage:
    size: 5Gi
    storageClass: ceph-block
  superuserSecret:
    name: cloudnative-pg-secret
  enableSuperuserAccess: true
  postgresql:
    parameters:
      checkpoint_timeout: 15min
      max_wal_size: 4GB
      max_connections: "400"
      min_wal_size: 1GB
      shared_buffers: 128MB
      wal_compression: "on"
  nodeMaintenanceWindow:
    inProgress: false
    reusePVC: true
  resources:
    requests:
      cpu: 500m
    limits:
      memory: 4Gi
      hugepages-2Mi: 2Gi # Requires sysctl set on the host
  monitoring:
    enablePodMonitor: true
  # Using the barman-cloud plugin for backups (required for CNPG 1.26+)
  # Note: serverName version needs to be incremented when recovering from an existing cnpg cluster
  plugins:
  - name: barman-cloud.cloudnative-pg.io
    isWALArchiver: true
    parameters:
      barmanObjectName: r2
      serverName: postgres17-v1
  backup:
    volumeSnapshot:
      className: csi-ceph-blockpool
      walClassName: csi-ceph-blockpool
      snapshotOwnerReference: cluster
      onlineConfiguration:
        # Finish the standby restartpoint before recovery can cancel backup startup.
        immediateCheckpoint: true
  # Note: Uncomment bootstrap section to recover from backup
  # After successful migration, comment this out and increment serverName in ObjectStore
  # bootstrap:
  #   recovery:
  #     source: &previousCluster postgres17-v1
  # Note: externalClusters is needed when recovering from an existing cnpg cluster
  # externalClusters:
  # - name: *previousCluster
  #   plugin:
  #     name: barman-cloud.cloudnative-pg.io
  #     parameters:
  #       barmanObjectName: r2
  #       serverName: *previousCluster
