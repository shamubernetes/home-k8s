---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n-operator
  namespace: services
  labels:
    app.kubernetes.io/name: n8n-operator
    app.kubernetes.io/component: controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: n8n-operator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: n8n-operator
        app.kubernetes.io/component: controller
    spec:
      serviceAccountName: n8n-operator
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: manager
          image: ghcr.io/shamubernetes/n8n-operator:0.6.3@sha256:266a53096fda45a24cdf5d11906134905018d39a602df29e3d7154b3c5e254cf
          args:
            - --leader-elect
            - --health-probe-bind-address=:8081
            - --metrics-bind-address=:8080
          ports:
            - name: metrics
              containerPort: 8080
              protocol: TCP
            - name: health
              containerPort: 8081
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 10m
              memory: 64Mi
      terminationGracePeriodSeconds: 10
