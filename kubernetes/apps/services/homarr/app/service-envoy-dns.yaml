apiVersion: v1
kind: Service
metadata:
  name: homarr-envoy-dns
  annotations:
    external-dns.alpha.kubernetes.io/hostname: dash.${HOME_DOMAIN}
    external-dns.alpha.kubernetes.io/target: ${IPAM_IP_ENVOY_INTERNAL}
spec:
  type: ExternalName
  externalName: envoy-internal.network.svc.cluster.local
