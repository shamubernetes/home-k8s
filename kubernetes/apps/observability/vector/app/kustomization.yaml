# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Verification-only PR for the automatic image-availability path.
resources:
- ./agent
- ./aggregator
