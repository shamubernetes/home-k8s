# Flux Operator chart values are shared by the GitOps HelmRelease and talos/helmfile recovery path.
image:
  repository: ghcr.io/controlplaneio-fluxcd/flux-operator
  # renovate: datasource=docker depName=ghcr.io/controlplaneio-fluxcd/flux-operator
  tag: v0.58.1@sha256:d84b113c3eadc229157444570304148a0d29ed8a04142349010356928b0448c4
priorityClassName: system-cluster-critical
serviceMonitor:
  create: true
  interval: 60s
  scrapeTimeout: 30s
web:
  enabled: false
  networkPolicy:
    create: false
  rbac:
    createRoles: false
