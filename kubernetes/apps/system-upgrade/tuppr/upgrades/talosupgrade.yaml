# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/tuppr.home-operations.com/talosupgrade_v1alpha1.json
apiVersion: tuppr.home-operations.com/v1alpha1
kind: TalosUpgrade
metadata:
  name: talos
  annotations:
    # K8S-63: resume only in supervised maintenance after playback is idle.
    tuppr.home-operations.com/suspend: "playback-maintenance-pause"
spec:
  nodeSelector:
    matchLabels:
      kubernetes.io/hostname: k8s-rhea
  talos:
    # renovate: datasource=github-releases depName=siderolabs/talos
    version: v1.13.10
  policy:
    placement: hard
    rebootMode: powercycle
    waitForVolumeDetach: true
  healthChecks:
  - apiVersion: volsync.backube/v1alpha1
    kind: ReplicationSource
    expr: |-
      status.conditions.filter(c, c.type == "Synchronizing").all(c, c.status == "False")
  - apiVersion: ceph.rook.io/v1
    kind: CephCluster
    expr: |-
      status.ceph.health in ['HEALTH_OK']
  - apiVersion: comcast.github.io/v1
    kind: KuberhealthyState
    name: plex-no-streams
    namespace: observability
    description: No active Plex streams
    expr: |-
      object.spec.OK == true
