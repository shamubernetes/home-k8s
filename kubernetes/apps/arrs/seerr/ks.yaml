# yaml-language-server: $schema=https://k8s-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app seerr
  namespace: "${KS_NAMESPACE}"
spec:
  targetNamespace: &namespace arrs
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  dependsOn:
  - name: external-secrets-secret-store
  - name: kopiur
  - name: rook-ceph-cluster
  - name: snapshot-controller
  path: ./kubernetes/apps/arrs/seerr/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: "${KS_GIT_REPO_NAME}"
  wait: false
  # SnapshotReplication readiness is monitored separately because schedule-only
  # updates can leave observedGeneration stale on otherwise healthy resources.
  healthChecks:
  - apiVersion: external-secrets.io/v1
    kind: ExternalSecret
    name: seerr-kopiur-nas
    namespace: arrs
  - apiVersion: external-secrets.io/v1
    kind: ExternalSecret
    name: seerr-kopiur-r2
    namespace: arrs
  - apiVersion: kopiur.home-operations.com/v1alpha1
    kind: Repository
    name: seerr-nas-smb
    namespace: arrs
  - apiVersion: kopiur.home-operations.com/v1alpha1
    kind: Repository
    name: seerr-r2
    namespace: arrs
  - apiVersion: kopiur.home-operations.com/v1alpha1
    kind: SnapshotPolicy
    name: seerr
    namespace: arrs
  - apiVersion: kopiur.home-operations.com/v1alpha1
    kind: SnapshotSchedule
    name: seerr-daily
    namespace: arrs
  interval: "${KS_INTERVAL}"
  retryInterval: "${KS_RETRY_INTERVAL}"
  timeout: "${KS_TIMEOUT}"
  postBuild:
    substitute:
      APP: *app
      NAMESPACE: *namespace
      GATUS_SUBDOMAIN: requests
      GATUS_PATH: /api/v1/status
      VOLSYNC_CAPACITY: 5Gi
      VOLSYNC_SCHEDULE: "${VOLSYNC_SCHEDULE_SEERR}"
      VOLSYNC_R2_SCHEDULE: "${VOLSYNC_R2_SCHEDULE_SEERR}"
